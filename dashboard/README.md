# Dashboard configuration
This is the dashboard file for the You must apply the manifest with the -n kubernetes-dashboard flag

## Dependencies
The following direct dependencies must have been installed in the cluster, they might have additional dependencies:
* sealed secrets
* istio
* certs/dashboard-certificate
